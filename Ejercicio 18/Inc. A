var valores = [true, 5, false, "hola", "adios", 2];
var max = 0;

for (let i=0; i < valores.length; i++) {
 
        if (valores[i].length > max) {
            var res = valores[i];
            max = valores[i].length
        }   
}

console.log("el String mas largo es " + res);
